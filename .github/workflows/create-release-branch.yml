name: Create Release Branch

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  create-release-branch:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Fetch tags
        run: git fetch --prune --unshallow --tags || git fetch --tags

      - name: Run Release Please on dev
        id: rp
        uses: googleapis/release-please-action@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          release-type: simple
          target-branch: dev

      # Se o Release Please criou a PR e a branch automática, cria PR direcionada para main
      - name: Create PR from release branch to main
        if: ${{ steps.rp.outputs.pr && steps.rp.outputs.pr-branch }}
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prBranch = `${{ steps.rp.outputs.pr-branch }}`;
            const prUrl = `${{ steps.rp.outputs.pr-url }}`;
            const prNumber = `${{ steps.rp.outputs.pr }}`;
            console.log(`Release Please PR on dev: ${prUrl} (#${prNumber})`);

            // Create clean promotion PR to main using the generated branch
            const { data: newPr } = await github.rest.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              head: prBranch,
              base: "main",
              title: `Promote release from dev (${prBranch})`,
              body: `Automated promotion of Release Please changes from dev via branch ${prBranch}.\nSource PR: ${prUrl}`
            });
            console.log(`Created promotion PR to main: ${newPr.html_url}`);

      # Se não criou PR, falha com instruções úteis
      - name: Fail when no PR created on dev
        if: ${{ !steps.rp.outputs.pr }}
        run: |
          echo "Release Please não criou uma PR em dev. Verifique se há commits com Conventional Commits que gerem release (feat:, fix:, chore:) na branch dev, e se a configuração do Release Please está correta."
          exit 1

      # Extrai versão com fallback para tag_name ou version, removendo prefixo 'v'
      - name: Extract new version
        id: version
        run: |
          RP_VERSION="${{ steps.rp.outputs.version }}"
          TAG_NAME="${{ steps.rp.outputs.tag_name }}"
          MAJOR="${{ steps.rp.outputs.major }}"
          MINOR="${{ steps.rp.outputs.minor }}"
          PATCH="${{ steps.rp.outputs.patch }}"

          VERSION=""
          if [ -n "$MAJOR" ] && [ -n "$MINOR" ] && [ -n "$PATCH" ]; then
            VERSION="${MAJOR}.${MINOR}.${PATCH}"
          elif [ -n "$RP_VERSION" ]; then
            VERSION="$RP_VERSION"
          elif [ -n "$TAG_NAME" ]; then
            VERSION="$TAG_NAME"
          fi

          if [ -z "$VERSION" ]; then
            echo "Não foi possível determinar a versão a partir do Release Please."
            exit 1
          fi

          # Remove prefixo 'v' se existir (ex.: v1.2.3 -> 1.2.3)
          STRIPPED_VERSION="${VERSION#v}"

          echo "Detected version: $STRIPPED_VERSION"
          echo "version=$STRIPPED_VERSION" >> $GITHUB_OUTPUT

      - name: Prepare release branch name
        id: branch
        run: |
          BRANCH="release/${{ steps.version.outputs.version }}"
          echo "branch=$BRANCH" >> $GITHUB_OUTPUT
          echo "Criando branch: $BRANCH"

      # Cria a release/x.x.x baseada na branch automática gerada pelo Release Please
      - name: Create proper release branch
        run: |
          AUTO_BRANCH="${{ steps.rp.outputs.pr-branch }}"
          if [ -z "$AUTO_BRANCH" ]; then
            echo "Release Please não criou nenhuma PR/branch automática (pr-branch vazio)."
            echo "Verifique se houve mudanças que gerem release ou se o Release Please está configurado para criar PR."
            exit 1
          fi

          git checkout "$AUTO_BRANCH"
          git pull
          git checkout -b "${{ steps.branch.outputs.branch }}"
          git push origin "${{ steps.branch.outputs.branch }}"

      # Cria PR limpa direcionada para main
      - name: Create clean PR (release/x.x.x -> main)
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const version = "${{ steps.version.outputs.version }}";
            const branch = `release/${version}`;

            await github.rest.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              head: branch,
              base: "main",
              title: `Release ${version}`,
              body: `This PR promotes version **${version}** to main.`
            });
